# TICKET-11: Internacionalização (i18n)

**Status**: 🔴 TODO
**Prioridade**: 🟢 BAIXA
**Estimativa**: 1 dia
**Dependências**: TICKET-04

---

## 📋 Objetivo

Portar sistema de idiomas (PT-BR, EN, JA, DE) do Java para TypeScript.

**Arquivo de referência**: `src/ilcompiler/edit/Language.java` (256 linhas)

---

## ✅ Tarefas

### 1. Criar arquivos de tradução

```typescript
// src/i18n/translations.ts

export const translations = {
  'pt-BR': {
    file: 'Arquivo',
    save: 'Salvar',
    load: 'Carregar',
    edit: 'Editar',
    help: 'Ajuda',
    about: 'Sobre',
    code: 'Código',
    inputs: 'Entradas',
    outputs: 'Saídas',
    delay: 'Tempo de delay (ms):',
    start: 'Iniciar',
    stop: 'Parar',
    refresh: 'Atualizar',
  },
  en: {
    file: 'File',
    save: 'Save',
    load: 'Load',
    edit: 'Edit',
    help: 'Help',
    about: 'About',
    code: 'Code',
    inputs: 'Inputs',
    outputs: 'Outputs',
    delay: 'Time delay (ms):',
    start: 'Start',
    stop: 'Stop',
    refresh: 'Refresh',
  },
  ja: {
    file: 'ファイル',
    save: '保存',
    load: 'ロードするには',
    edit: '編集',
    help: 'ヘルプ',
    about: 'について',
    code: 'コード',
    inputs: '入力',
    outputs: '出力',
    delay: '遅延時間 (ミリ秒):',
    start: '開始',
    stop: '停止',
    refresh: '更新',
  },
  de: {
    file: 'Datei',
    save: 'Speichern',
    load: 'Zum Laden',
    edit: 'Bearbeiten',
    help: 'Helfen',
    about: 'Um',
    code: 'Code',
    inputs: 'Eingaben',
    outputs: 'Ausgänge',
    delay: 'Zeitverzögerung (ms):',
    start: 'Start',
    stop: 'Halt',
    refresh: 'Aktualisieren',
  },
} as const

export type Language = keyof typeof translations
export type TranslationKey = keyof typeof translations['pt-BR']
```

### 2. Hook useLanguage

```typescript
// src/hooks/useLanguage.ts
import { create } from 'zustand'
import { translations, Language, TranslationKey } from '../i18n/translations'

interface LanguageStore {
  language: Language
  setLanguage: (lang: Language) => void
  t: (key: TranslationKey) => string
}

export const useLanguage = create<LanguageStore>((set, get) => ({
  language: 'pt-BR',
  setLanguage: (lang) => set({ language: lang }),
  t: (key) => {
    const { language } = get()
    return translations[language][key] || key
  },
}))
```

### 3. Language Selector Component

```typescript
// src/components/LanguageSelector.tsx
import { useLanguage } from '../hooks/useLanguage'
import { Language } from '../i18n/translations'

const languages: { code: Language; name: string; flag: string }[] = [
  { code: 'pt-BR', name: 'Português', flag: '🇧🇷' },
  { code: 'en', name: 'English', flag: '🇺🇸' },
  { code: 'ja', name: '日本語', flag: '🇯🇵' },
  { code: 'de', name: 'Deutsch', flag: '🇩🇪' },
]

export function LanguageSelector() {
  const { language, setLanguage } = useLanguage()

  return (
    <select
      value={language}
      onChange={(e) => setLanguage(e.target.value as Language)}
      className="language-selector"
    >
      {languages.map((lang) => (
        <option key={lang.code} value={lang.code}>
          {lang.flag} {lang.name}
        </option>
      ))}
    </select>
  )
}
```

### 4. Usar em componentes

```typescript
import { useLanguage } from '../hooks/useLanguage'

export function MyComponent() {
  const { t } = useLanguage()

  return (
    <div>
      <h1>{t('file')}</h1>
      <button>{t('save')}</button>
    </div>
  )
}
```

---

## 🎯 Critérios de Aceitação

- [ ] 4 idiomas disponíveis (PT, EN, JA, DE)
- [ ] Troca de idioma funciona
- [ ] Textos atualizam instantaneamente
- [ ] Estado persiste (localStorage)
- [ ] Todos os componentes traduzidos

---

## 📝 Notas

- Usar Zustand para state global leve
- Ou Context API se preferir
- Persistir escolha no localStorage

---

## 🔗 Referência

- `src/ilcompiler/edit/Language.java` (256 linhas)
